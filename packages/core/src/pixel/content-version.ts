/**
 * Default contentVersion string when caller does not pass catalog version.
 * Wiki client should pass `@suite/content` CONTENT_VERSION explicitly.
 */
export const CONTENT_VERSION_PLACEHOLDER = '0.0.0-unset' as const;
